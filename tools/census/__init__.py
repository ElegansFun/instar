"""Census converter + verifier. Python standard library only, by design:
anyone can re-verify a registered Merkle root with a bare Python install."""

CONVERTER_VERSION = "census-converter/0.1.0"
FORMAT_VERSION = "0.1.0"
